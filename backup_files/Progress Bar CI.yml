# 本来是用来更新时间剩余的，但是每天都会显示无意义的贡献??? 还是放这里吧，以后再用 --> 放在workflow下面去掉注释就可以使用了
# name: Progress Bar CI

# on:
#   workflow_dispatch:
#   schedule:
#     - cron: '0 */6 * * *'

# jobs:
#   build:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v2
#     - name: Use Node.js
#       uses: actions/setup-node@v1
#       with:
#         node-version: '14.x'
#     - name: Update README.md
#       run: node index.js > README.md
#     - name: Commit change & Push
#       run: |
#           git config user.name 'github-actions[bot]'
#           git config user.email '1336143736+github-actions[bot]@users.noreply.github.com'
#           git commit -am "bot: update README.md automatically"
#           git push
